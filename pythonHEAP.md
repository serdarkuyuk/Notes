# Heap in Python

```python
import heapq

heapq.heapify(list)  # make heap structure

heapq.heappush(list, number)

heapq.heappop(list) # pop the root element (smallest element)

heapq.heappushpop(list, number)
